name: CI
on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker compose version
      - run: docker compose build
      - run: docker compose --compatibility run --rm --no-deps app pytest -v
